package httpsender

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestHTTPSender(t *testing.T) {
	Convey("Given an HTTP sender configuration", t, func() {
		Convey("Create a new instance of a sender", func() {
		})
	})
}
